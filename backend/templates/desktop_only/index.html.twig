{% extends 'base.html.twig' %}

{% block title %}Fonctionnalités Desktop - My Ankode{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" href="{{ asset('css/desktop-only.css') }}">
{% endblock %}

{% block body %}
<div class="desktop-only-container">
    {# Header avec icône #}
    <div class="desktop-only-header">
        <svg class="desktop-only-icon" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.75 17L9 20l-1 1h8l-1-1-.75-3M3 13h18M5 17h14a2 2 0 002-2V5a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z" />
        </svg>
        <h1>{ Fonctionnalités Desktop }</h1>
    </div>

    {# Message principal #}
    <div class="desktop-only-message">
        <p class="lead">
            Certaines fonctionnalités de <strong>My Ankode</strong> sont conçues spécifiquement 
            pour une <span class="highlight">utilisation desktop</span> afin de garantir 
            la meilleure expérience utilisateur possible.
        </p>
    </div>

    {# Liste des fonctionnalités #}
    <div class="features-grid">
        {% for feature in desktopFeatures %}
        <div class="feature-card">
            <div class="feature-icon">
                <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="{{ feature.icon }}" />
                </svg>
            </div>
            <h3>{{ feature.name }}</h3>
            <p>{{ feature.reason }}</p>
        </div>
        {% endfor %}
    </div>

    {# Call to action #}
    <div class="desktop-only-cta">
        <p>
            Pour accéder à ces fonctionnalités, 
            <strong>connecte-toi depuis un ordinateur</strong> ! 
        </p>
        <a href="{{ path('app_dashboard') }}" class="btn-back">
            Retour au Dashboard
        </a>
    </div>
</div>
{% endblock %}