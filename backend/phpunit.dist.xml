<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="tests/bootstrap.php"
         colors="true"
         failOnWarning="true"
         failOnRisky="true">
    
    <!-- 
    ==========================================
    TESTSUITES - Organisation des tests
    ==========================================
    Grouper les tests par type pour exécution sélective
    - Entity : tests PostgreSQL (User, Project, Task, Competence)
    - Controller : tests endpoints API (CRUD, auth)
    - Document : tests MongoDB (Article, Snippet)
    - Security : tests sécurité (CSRF, ownership, validators)
    -->
    <testsuites>
        <testsuite name="Entity">
            <directory>tests/Entity</directory>
        </testsuite>
        <testsuite name="Controller">
            <directory>tests/Controller</directory>
        </testsuite>
        <testsuite name="Document">
            <directory>tests/Document</directory>
        </testsuite>
        <testsuite name="Security">
            <directory>tests/Security</directory>
        </testsuite>
    </testsuites>

    <!-- Configuration du Code Coverage -->
    <coverage>
        <report>
            <html outputDirectory="coverage"/>
            <text outputFile="php://stdout" showOnlySummary="true"/>
        </report>
    </coverage>

    <!-- 
    ==========================================
    VARIABLES PHP - Environnement de test
    ==========================================
    Isolation complète de l'environnement test
    - APP_ENV=test : config/packages/test/
    - Base dédiée : my_ankode_test
    - Fixtures rechargées avant chaque test
    -->
    <php>
        <ini name="display_errors" value="1" />
        <ini name="error_reporting" value="-1" />
        <server name="APP_ENV" value="test" force="true" />
        <server name="SHELL_VERBOSITY" value="-1" />
        <server name="SYMFONY_PHPUNIT_REMOVE" value="" />
        <server name="SYMFONY_PHPUNIT_VERSION" value="12.5" />
    </php>

    <!-- 
    ==========================================
    SOURCE - Code coverage
    ==========================================
    Analyse tous les .php de src/
    SAUF DataFixtures (données test) et Kernel.php (Symfony généré)
    -->
    <source>
        <include>
            <directory suffix=".php">src</directory>
        </include>
        <exclude>
            <directory>src/DataFixtures</directory>
            <file>src/Kernel.php</file>
        </exclude>
    </source>
</phpunit>